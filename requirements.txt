# Core pipeline
arxiv
semanticscholar
anthropic
pydantic>=2.0
python-dotenv

# Audio
elevenlabs
pydub
audioop-lts           # Python 3.13 backport of audioop (required by pydub)

# Voice providers (alternatives to ElevenLabs)
openai                              # VOICE_PROVIDER=openai
# google-cloud-texttospeech         # VOICE_PROVIDER=google (uncomment to install)

# Graph
networkx
lxml

# Web
fastapi
uvicorn[standard]
python-multipart

# Distribution (Phase 4)
feedgen

# Scheduling (Phase 3)
apscheduler

# Testing
pytest
pytest-asyncio
pytest-mock
httpx
respx
